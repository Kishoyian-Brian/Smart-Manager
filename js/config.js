/**
 * API configuration for Smart City Waste Management
 * Set API_BASE to your backend URL; leave empty to use localStorage (offline mode)
 */
window.API_BASE = window.API_BASE || 'http://localhost:3000/api';
